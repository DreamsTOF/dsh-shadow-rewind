# dsh-shadow-rewind 功能互斥（左脑打右脑）分析

> 目的：把「几个功能的实现互相冲突 / 重复实现同一职责」的点找出来，
> 为每个冲突组列出取舍方案与各自代价，供你评估后再决定改不改、怎么改。
> 本文只做分析，不包含任何代码改动。

- 分析对象：`dsh-shadow-rewind`（0.9.2，宿主 + 浏览器客户端）
- 分析日期：2026-09
- 分析方法：通读宿主端（`engine.ts` / `rewind-host.ts` / `store.ts` / `capture.ts` /
  `manifest.ts` / `write-gate.ts` / `command-windows.ts` / `attribution.ts` /
  `trace-replay.ts` / `file-review/*`）+ 客户端（`session-changes.ts` /
  `FileReviewTab.tsx` / `fs-diff-utils.ts` / `ProducedFiles.tsx` / `live-bar.tsx` /
  `rewind.ts` / `turn-deliverables.ts` / `deleted-paths.ts` / `timeline-panel.tsx`）

---

## 0. 一句话根因

插件试图让两套互相独立的「变更观测」在同一个产品词汇（每轮改了什么、
可撤销、可恢复）下共存，却没有一个**统一的权威判定 + 归并去重层**：

1. **工具可见性管线** —— 从 `tool/call` / `tool/result` / 录制器推导，精确但
   只覆盖模型直发的 `write` / `edit` / `str_replace_editor` / `run_code`；
2. **影子快照 / 树对比管线** —— 从轮起/轮末检查点的**整树 diff** 推导，完整但
   对模型工具、终端命令、外部进程一视同仁，不知道「是谁、用什么改的」。

两套都声称能回答「这一轮改了什么」，结果在 UI 上被并排渲染成同一条事实。

---

## 冲突组 A —— 同一条「轮变更」被两套数学各算一遍（最重）

### 冲突双方

| 面 | 实现 | 覆盖 | 数学 |
| --- | --- | --- | --- |
| 工具视图 | `turn-deliverables.ts` + `session-changes.ts` + `host.ts` 录制器 | 模型直发的工具调用；终端命令只贡献 `deleted-paths.ts` 的**词法删除展示** | 解析 `tool/call` 参数与 `meta.diffs` |
| 检查点对比 | `rewind-host.ts` 的 `computeTurnFsChanges`（供 `/fs-changes`） | 轮起→轮末的**一切**文件系统写盘（工具 + 终端 + 外部） | `diffTrees(轮起 entries, 轮末 entries)`，再按写入闸开关做归属过滤 |

### 具体互斥点

- 一次成功的模型 `write` 会**同时**被两条管线捕获：
  - 工具侧给的是工具自报的 hunks（`meta.diffs`）；
  - fs 侧给的是检查点整文件 diff。
  - 客户端合并时对同路径规则是「工具条目优先、fs 条目跳过」（`live-bar.tsx`
    `seen` 过滤、`ProducedFiles.tsx` `reviews` 合并、`FileReviewTab.tsx`
    `turns` 合并）——但**两条管线对同一文件的 diff 内容并不相同**，UI 只显示
    工具那条，fs 那条被静默丢弃，二者从未对账。
- 两处对「同路径工具与 fs 都改过」的取舍互相矛盾：
  - `computeTurnFsChanges`（闸开）在**服务端**把非 target 独有路径剔除；
  - 客户端在**渲染端**用「fs 跳过工具已有路径」覆盖 fs 条目。
  - 两者采用相反的优先方向，改任一端的判定都会让另一端语义漂移。
- live 条与轮尾卡片各自独立做同一份合并（各自 `warm` 缓存 vs 快照推导），
  轮进行中可能出现「一卡有该文件、另一卡没有」。

### 代价 / 风险

- 同一次修改可能被展示成**不同行数、不同 diff**（取决于打开哪条管线）。
- 用户撤销「工具条目的 hunk」后，fs 条目的整文件 diff 仍显示 applied/undone，
  两侧状态互相矛盾。
- 维护成本高：任何一次「模型直发 + 终端」混合改同一文件，都要在两处各猜一次。

### 取舍方案

**方案 A1 —— fs 检查点对比为唯一真相（推荐）**
把「每轮 fs 变更」收敛为检查点 diff 唯一权威；工具视图 hunks 只作为同路径
「可撤销块」的标注层，不作为独立清单。

- 代价：需把 fs 管线补上工具调用归属（「哪条命令/哪个工具改的」从工具事件
  挂靠到 diff 条目，替代现在的工具自报）；时间线里对**无轮末快照的历史轮**
  的兜底重放价值保留（见冲突组 B）。
- 好处：删除/新增/权限位/空目录/终端写盘全部统一到一套存在性语义；撤销状态
  巡检只需对 fs 条目做，不再两套状态互相打架。
- 风险：改动面大（客户端合并逻辑 + 服务端 fs 端点 + 巡检），需要回归
  「工具自报 hunk vs 检查点 diff」在 CRLF / 大文件 / 损坏快照下的差异。

**方案 A2 —— 工具视图为权威，fs 降级为「工具看不到的写盘」补充**
fs 管线只报工具视图没有覆盖到的路径。

- 代价：需要做真正的**同路径对账**：两侧内容相等才跳过，不等则标冲突/双条目，
  否则仍会漏报终端命令与工具改同一文件时的真实差异。
- 好处：改动面比 A1 小（主要改客户端合并 + 服务端 fs 端点的过滤语义对齐）。
- 风险：fs 管线的存在意义大打折扣；「快照优先、轨迹兜底」的产品叙事要改写。

**方案 A3 —— 维持现状**
- 代价：互斥点继续累积，撤销巡检与整树恢复永远不知道对方做了什么
  （见冲突组 D 的 `snapshotDialogWarn` 文案，它已经承认了这一点）。

---

## 冲突组 B —— 快照能覆盖的事，轨迹重放又做了一遍

### 冲突双方

- **影子快照对比**：任意两个 turn 检查点 → 完整逐文件树 diff（`rp_…` 寻址）。
- **轨迹重放**（`trace-replay.ts`）：从会话事件**重放** `write` / `edit` /
  `str_replace_editor` 内容 → 区间 diff（`trace:<seq>` 寻址）。

两者被放进同一个时间线浮层（`timeline-panel.tsx`），并刻意**禁止混用**
（`rewind-host.ts` `handleTraceHttp` 与 `/shadow-diff` 命令均校验
`trace:` 与 `rp_` 不可混搭）。

### 具体互斥点

- 对同一对轮边界：
  - 选「轮起/轮末检查点」→ 拿到完整真相（含终端与外部写盘）；
  - 选两条 `trace:` 节点 → 拿到**不完整的重放近似**。
- 轨迹重放有固有盲区且自认天花板（`trace-replay.ts` 顶部 TODO）：
  「只覆盖会话内首次 write 之后的状态；文件在会话开始前就存在但从未被
  内容型工具写过时，重放图里没有基线，区间 diff 会把它标成 added。」
- 即：**对工具改动它完整、对终端/外部它全盲、对历史基线它失真**，却和完整
  的快照对比在同一面板上被当成等价入口。

### 代价 / 风险

- 用户选错节点类型就会拿到一份**会误导的 diff**（把老文件标成新增）。
- 快照被配额淘汰 / 被 GC 后，时间线要靠轨迹兜底（这是它的真实价值）；
  但只要有快照在，同一对比走两条路结果不一致，增加排查成本。

### 取舍方案

**方案 B1 —— 保留双轨，但把「重放基线」用快照补齐（推荐）**
让轨迹重放以最近的轮起检查点为基线初始化重放图，消除「老文件标成 added」
的失真；同时在同一对比结果里附 notes 说明覆盖盲区。

- 代价：`trace-replay.ts` 需要接一个「读检查点条目补基线」的入口（数据已在
  引擎，改动集中在宿主端 + 端点参数）。
- 好处：双轨语义各自补全，时间线两种入口结果趋于一致，盲区诚实标注的叙事
  可以继续成立。
- 风险：快照不存在时（兜底场景）仍退回当前的不完整语义——这是可接受的降级。

**方案 B2 —— 收敛到快照一种寻址**
时间线只做检查点对比，删掉 `trace:` 寻址与 `/shadow-diff` 的 trace 分支。

- 代价：历史会话、被配额淘汰的轮、GC 后的回看能力**直接消失**（这是当初引入
  轨迹重放的原因）；改动涉及 host 端点 + timeline-panel + 命令面 + 测试。
- 好处：模型从两套变一套，概念负担最小。

**方案 B3 —— 维持现状**
- 代价：用户教育成本（记住哪种节点能选、选完得到什么）+ 失真 diff 的风险持续。

---

## 冲突组 C —— 「删除」被两套机制处理成两个结果

### 冲突双方

1. **词法删除**（`deleted-paths.ts`）：从终端命令行**逐字**抽取
   `rm` / `Remove-Item` / `del` / `rd` 等参数位 → 产出
   「**无 hunk、不能撤销、仅展示**」的条目（`deleted: true, diffs: []`）。
2. **检查点 fs 对比**：同一轮文件被删，产出**可完整撤销**的整文件
   `deleted` 形状（`oldText=内容, newText=''`），宿主能写回旧内容。

客户端在多处明确区分两者（`FileReviewTab.tsx` 的 `inspectable`、
`ProducedFiles.tsx` 的 `reversiblePaths` 都以「删除但带全文 = 可撤销」为准），
说明作者清楚这是两条路，但产品层仍把它们画成同一种「已删除」状态。

### 具体互斥点

- 同一个 `rm` 行为可能**双份上报**（fs 管线认为文件没了 + 词法管线也认出了），
  也可能**只词法不 fs**（闸开时 fs 过滤剔除，或 fs-changes 尚未 warm）；
- 对「rm -r 目录」词法报目录自身、fs 报整棵子树，颗粒度也不一致；
- 用户看到一个「已删除」行，无法从外观判断它能否还原；点了撤销后，
  fs 占位条目的可还原性又会随全文补齐而变化。

### 代价 / 风险

- 「删除不可撤销」与「删除可撤销」并存，违背 README 宣称的
  「三类变更全部可见可撤销」；
- 词法解析器是保守启发式（含通配符/展开就整段作废），误报漏报都会造成
  与 fs 真相不一致的展示。

### 取舍方案

**方案 C1 —— 让词法删除只做「快速提示」，最终以 fs 为准（推荐）**
词法删除条目不再作为独立「变更」，而只作为 fs 变更**未就绪时**的占位提示；
一旦 fs 管线给出了该路径的删除/恢复证据，即用 fs 条目顶替并解锁撤销。

- 代价：需要客户端做「词法占位 → fs 证据」的替换规则 + 降级文案；fs 缺失
  （历史轮 / 配额淘汰）时删除仍退回只读展示。
- 好处：单条删除的「可撤销性」只有一个权威（fs），消灭双轨展示。
- 风险：改动集中在客户端合并与占位生命周期，宿主侧基本不动。

**方案 C2 —— 删掉词法删除管线**
- 代价：终端删除在 fs 证据到位前（live 轮、历史轮）完全不可见；
  且 fs 端点本身也被闸开的归属过滤剔除部分路径，可见性会退化。
- 好处：实现最简。

**方案 C3 —— 维持现状**
- 代价：「删除」词汇永远有两个互相矛盾的实现，且两边的真值不共享。

---

## 冲突组 D —— 写入闸 vs 单条 fs 撤销 / 词法删除的管辖重叠

### 冲突双方

- **整树恢复**：`engine.applyRestore` 带全套安全闸，恢复前经
  `partitionRunningSessions` 按写入闸分诊阻塞运行中的会话（`applyGuarded`）。
- **单条/按轮文件撤销**：`FileReviewService.apply`（Typert `fileReview`）逐路径
  CAS + 工作区围栏，但**完全不知道写入闸、也不做运行中会话占用检查**。

### 具体互斥点

- 同一会话在「别的会话正占用工作区」时：
  - 走整树恢复 → 被 `WORKSPACE_IN_USE` 拦下；
  - 走侧边栏/卡片的单条 fs 撤销 → 直接执行（`runMaintenance` 只保证不打断
    自己会话的回合，不查别的会话）。
- `locales.ts` 的 `snapshotDialogWarn` 自己承认：整树恢复会让
  「已记录的块级撤销状态可能随之失效（显示为冲突）」——两个撤销系统
  **互相不知道对方的存在**。
- 词法删除（冲突组 C）来自终端参数，但写入闸开时该终端调用根本不会执行，
  删除路径却可能因历史事件仍被展示，与 fs 检查点（真实写盘才产生）语义打架。

### 代价 / 风险

- 多会话场景下，单条撤销可以绕过「以当前为准」的所有权语义，把另一会话
  刚写的内容静默覆盖掉——只是碰运气被逐文件 CAS 兜住（CAS 只管字节，
  不管「该不该由你改」）。
- README 明说写入闸边界是「只管 DSH 工具调用」——但 file-review 的 apply
  走的也是 DSH 工具侧路径，却不在闸内，属于边界内的遗漏。

### 取舍方案

**方案 D1 —— 单条/按轮撤销并入写入闸语义（推荐）**
让 `FileReviewService.apply` 在空闲窗口检查目标工作区的所有权：
非所有者会话对非本会话归属路径的开关要么拒绝、要么标记为「需显式确认」
（对称模式已具备的 `autoSelect/multi` 弹窗可以复用）。

- 代价：apply 路径要拿到「目标会话 + 工作区所有者」的查询（write-gate 已
  暴露 `ownerOf`，改动不大）；对称模式下要允许显式确认放行，不能一刀切。
- 好处：写入闸的「以当前为准」承诺真正覆盖所有写盘入口，不是只有整树恢复。
- 风险：需要写 gate + file-review 的联合测试（现有 write-gate.test 只测
  工具裁决，不测 file-review apply）。

**方案 D2 —— 在 UI 层提示「整树恢复会破坏已记录撤销」**
不改服务端，恢复确认对话框里把「已记录的块级撤销将失效」做成强提示 + 列清单。

- 代价：治标不治本；两个系统的状态模型依旧互不知晓。
- 好处：改动最小，先消除「静默失效」的体验坑。

**方案 D3 —— 维持现状**
- 代价：多会话 + 恢复 + 撤销的组合场景下行为不一致，需长期在 README 里
  解释「哪些撤销会被哪些恢复打断」。

---

## 冲突组 E —— Code Mode 录制与 fs 审计对同一 run_code 重复记账

### 冲突双方

- **录制器**（`host.ts` `tools/post-execute`）：按结果形状 `{path, before, after}`
  录制嵌套派发（run_code 子调用），并入「工具视图」轮；
- **fs 检查点对比**：同轮 run_code 引发的写盘同时出现在 fs 管线（只看树变了，
  不管写者）。

### 具体互斥点

- 同路径在两条管线都被计入；客户端合并靠「fs 跳过工具已有路径」兜住。
- README 边界自认：「Code Mode 录制按结果形状 `{path, before, after}` 识别
  嵌套修改，返回该形状的非文件工具会被误录。」
- 工具视图缺失 meta 的 run_code 改动，与 fs 侧整文件 diff 对不上（两侧 diff
  来源不同，见冲突组 A 的同一根因）。

### 取舍方案

**方案 E1 —— 跟随 A1 的收敛（推荐）**
若采用 A1（fs 为唯一真相），录制器只负责补「写入者是谁」的归属，不再作为
独立 diff 来源；若采用 A2（工具视图为权威），录制器保持，但 fs 对同路径
必须做内容对账后再决定跳过或合并。

- 代价：与 A 的改动绑定，无法单独推进。
- 好处：run_code 改动不再被两套机制各计一次。

**方案 E2 —— 收紧录制形状白名单**
把 `{path, before, after}` 形状识别限定到「确认为文件工具」的调用（配合工具名
或参数结构），消除误录。

- 代价：仍解决不了与 fs 的重复记账，只是少了误录的噪声。
- 好处：改动小、独立可做。

---

## 总结：建议的推进顺序（由你评估后决定）

| 顺序 | 动作 | 对应组 | 主要代价 |
| --- | --- | --- | --- |
| 1 | D1：把单条/按轮撤销并入写入闸语义 | D | 服务端 apply 路径 + 联合测试 |
| 2 | C1：词法删除降级为 fs 证据的占位 | C | 客户端占位生命周期 + 降级文案 |
| 3 | A1 或 A2：确立唯一权威变更源 | A/E | 客户端合并 + fs 端点过滤语义 + 全量回归 |
| 4 | B1：轨迹重放用快照补基线 | B | trace-replay 接引擎读检查点入口 |

第 1、2 步可独立落地、风险小；第 3 步是结构性收敛，决定 A、C、E 三组的最终
形态，建议先对齐方向再动；第 4 步是锦上添花，可与第 3 步解耦。

> 注意：以上引用均基于分析时点读到的源码；任何改动前请以最新代码为准。
